const express = require('express');
const router = express.Router();
const { protect } = require('../middleware/authMiddleware');


// Budget alerts shown in dashboard UI instead

module.exports = router;
