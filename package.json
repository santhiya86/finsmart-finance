{
  "name": "finsmart-finance",
  "version": "1.0.0",
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/server.js",
    "postinstall": "npm install --prefix backend"
  }
}
